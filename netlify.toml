[build]
  command = "npm run build"
  publish = ".next"

[context.production]
  branch = "main"

[context."feature/site-buildout"]
  command = "npm run build"
  publish = ".next"

[context.branch-deploy]
  command = "npm run build"
  publish = ".next"
